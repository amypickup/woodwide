/*

const client = require("../../client");
const sm = require("sitemap");

type UrlType = {
  url: string;
  changefreq: "daily" | "weekly" | "monthly";
  priority: number;
  img?: string;
};

const defaultUrls = [
  { url: "/", changefreq: "daily", priority: 1 },
  { url: "/recipes", priority: 0.5 },
  { url: "/posts", priority: 0.5 },
  { url: "/donate", priority: 0.1 },
];

*/

// TODO: generate sanityUrls

// TODO: return sitemap :/
